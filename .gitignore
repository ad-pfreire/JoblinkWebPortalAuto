
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
.claude/settings.local.json
.playwright-mcp/
specs/
handoff.md
bug-triage.md

# Personal learning guide (not shared with the team)
/learning/

# Environment variables
# Covers every per-environment file (.env.staging, .env.prestaging, .env.local,
# ...) - these hold real credentials and this repo is public. Only the
# placeholder template is tracked.
.env
.env.*
!.env.example
